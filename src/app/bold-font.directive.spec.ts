import { BoldFontDirective } from './bold-font.directive';

describe('BoldFontDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldFontDirective();
    expect(directive).toBeTruthy();
  });
});
